package com.botleague.backend.team.enums;

public enum RobotCategory {
    // New robot types (use these for all new robots)
    COMBAT_ROBOT,
    SOCCER_ROBOT,
    SUMO_ROBOT,
    LINE_FOLLOWER_ROBOT,
    TASK_ROBOT,
    RC_VEHICLE,
    DRONE,
    AIRCRAFT,
    INNOVATION_PROJECT,

    // Legacy values (kept for backward compatibility with older rows)
    COMBAT,
    RC,
    AUTONOMOUS
}
