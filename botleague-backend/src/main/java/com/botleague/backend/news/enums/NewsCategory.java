package com.botleague.backend.news.enums;

/** Editorial tag shown as the Create News modal's category pill row. Null on the entity = uncategorized. */
public enum NewsCategory {
    EVENTS_RECAP,
    GLOBAL,
    TEAM_SPOTLIGHT,
    TECH,
    UPDATE
}
