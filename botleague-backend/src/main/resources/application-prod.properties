# ── Production overrides ──────────────────────────────────────────────────
# Activated via SPRING_PROFILES_ACTIVE=prod (see docker-compose.prod.yml).

# Real frontend domain only — no localhost.
app.cors.allowed-origins=${APP_FRONTEND_URL:https://test.botleague.in}

spring.jpa.show-sql=false

logging.level.com.botleague=INFO
logging.level.root=WARN
